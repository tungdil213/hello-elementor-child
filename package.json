{
  "name": "hello-theme-child",
  "version": "1.0.0",
  "description": "=== Hello Elementor Child ===",
  "main": "index.js",
  "scripts": {
    "dev": "nodemon --ext js,scss,css --ignore dist/ --exec \"npm run build\"",
    "build": "node build.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "esbuild": "^0.11.1",
    "esbuild-plugin-sass": "^1.0.1",
    "nodemon": "^2.0.7"
  }
}
